{
  "name": "query-and-conquer",
  "version": "0.0.0",
  "private": true,
  "description": "Turn-based hex-grid strategy game",
  "type": "module",
  "scripts": {
    "dev": "live-server --port=8080 --no-browser",
    "maps:generate": "node scripts/generate-maps.js",
    "test": "node --test test/",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "live-server": "^1.2.2"
  }
}
