/**
 * Region-size, shallow-water-depth, and base-min-distance validators for map generation.
 * Stub only -- implemented in Stage 2 of the implementation plan.
 * Exists now so the module layout (and the canonical/visible-state
 * seam it supports) is established from Stage 1, not retrofitted later.
 */
export {};
