/**
 * Canonical state factory (new game): map, players, units, bases, turn info, schemaVersion.
 * Stub only -- implemented in Stage 3 of the implementation plan.
 * Exists now so the module layout (and the canonical/visible-state
 * seam it supports) is established from Stage 1, not retrofitted later.
 */
export {};
