/**
 * Drives AI turns at configurable speed; dispatches through the same commands as human input.
 * Stub only -- implemented in Stage 6 of the implementation plan.
 * Exists now so the module layout (and the canonical/visible-state
 * seam it supports) is established from Stage 1, not retrofitted later.
 */
export {};
