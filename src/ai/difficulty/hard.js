/**
 * Hard AI: the one documented exception reading canonical state directly; full pathfinding + target-priority.
 * Stub only -- implemented in Stage 7 of the implementation plan.
 * Exists now so the module layout (and the canonical/visible-state
 * seam it supports) is established from Stage 1, not retrofitted later.
 */
export {};
